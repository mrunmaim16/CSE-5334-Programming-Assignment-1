In this assignment, I have implemented a toy "search engine" in Python. The code will read a corpus and produce TF-IDF vectors for documents in the corpus. Then, given a query string, it will return the query answer--the document with the highest cosine similarity score for the query.
